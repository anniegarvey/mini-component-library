import { Meta, Story } from '@storybook/addon-docs/blocks';
import ProgressBar from './ProgressBar';

<Meta
  title="ProgressBar"
  component={ProgressBar}
  argTypes={{
    value: {
      control: {
        type: 'number',
        min: 0,
        max: 100,
      },
    },
    size: {
      control: {
        type: 'select',
        options: ['small', 'medium', 'large'],
      },
    },
    ariaLabel: {
      control: {
        type: 'text',
        description: 'Aria label'
      }
    },
    ariaLabelledBy: {
      control: {
        type: 'text',
        description: 'ID of element this progress bar is labelled by'
      }
    }
  }}
/>

export const Template = (args) => <ProgressBar {...args} />;

# ProgressBar

<Story name="Default" args={{ value: 50, size: 'medium', ariaLabel: 'Progress fetching available items' }}>
  {Template.bind({})}
</Story>
